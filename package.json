{
  "name": "scanni-web",
  "version": "1.0.0",
  "scripts": {
    "build": "if [ ! -d \"flutter\" ]; then git clone https://github.com/flutter/flutter.git -b stable; fi && ./flutter/bin/flutter build web --release --base-href /",
    "install": "echo 'No install needed'"
  }
}
